# calculator

Calculate mood expressions with `+ happiness` and `- sadness`.

## Example

```python
from calculator import calculate

result = calculate("+ happiness - sadness")
print(result)  # 0
```